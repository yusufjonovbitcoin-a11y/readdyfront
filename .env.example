VITE_SENTRY_DSN=

# API origin without trailing /api (paths already include /api/...).
# Wrong: VITE_API_BASE_URL=/api  → requests become /api/api/...
# Same-origin + nginx proxy: leave empty.
VITE_API_BASE_URL=
